<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.meatit">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/LightTheme">
        <activity android:name=".AfterIntroActivity"></activity>
        <activity android:name=".ChooseActivity" />
        <activity android:name=".MeatResultActivity" />
        <activity android:name=".AnimalSettingActivity" />
        <activity android:name=".CookSettingActivity" />
        <activity android:name=".DevicesAvailableActivity" />
        <activity android:name=".TroubleShootingActivity" />
        <activity android:name=".SearchActivity" />
        <activity
            android:name=".CreateAccountActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".LoginActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".WelcomeActivity"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".BeforeIntroActivity"
            android:screenOrientation="portrait" />
    </application>

</manifest>